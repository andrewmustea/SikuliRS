Separate all bank operations into its own class.
Separate all inventory operations into its own class.
Make a withdraw amount enum in withdrawFromBank()
Make a log function
Have an herb/picture class
Create custom exceptions 
Add a global flag class
Add auto collect from GE function
Put in more and better comments
Get keyboard interrupt

Add timeout to teamviewer
